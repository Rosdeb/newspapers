import 'package:flutter/material.dart';
class ArticalesDetails extends StatelessWidget {
  const ArticalesDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return ArticalesDetails(

    );
  }
}
